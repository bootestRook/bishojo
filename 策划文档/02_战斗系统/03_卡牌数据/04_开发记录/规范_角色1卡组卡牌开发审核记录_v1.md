# 规范：角色1卡组卡牌开发审核记录 V1

文件职责：记录 Agent C 对角色 1 卡组伙伴卡牌只读适配/验证层的审核结论、发现、验证证据、待确认项与归档状态。本文档只记录审核结果，不修改卡牌 JSON 或实现逻辑。

最后更新：2026-06-11
审核角色：Agent C
审核结论：PASS

---

## 1. 审核范围

| 类型 | 文件 |
|---|---|
| 适配器实现 | `tools/card_effect_adapter/adapter.mjs` |
| 验证入口 | `tools/card_effect_adapter/validate_role1_cards.mjs` |
| 开发状态总表 | `策划文档/02_战斗系统/03_卡牌数据/04_开发记录/总表_角色1卡组卡牌开发状态_v1.md` |
| 机制索引 | `策划文档/02_战斗系统/03_卡牌数据/04_开发记录/机制_角色1卡组卡牌机制拆解与实现矩阵_v1.md` |
| 单卡机制文件 | `策划文档/02_战斗系统/03_卡牌数据/04_开发记录/01_单卡机制矩阵/*.md` |
| 四张卡牌 Markdown | `策划文档/02_战斗系统/03_卡牌数据/01_角色1卡组/*.md` |
| 四张卡牌 JSON | `data/cards/partners/*.v1.json` |
| 底层规则 | `策划文档/02_战斗系统/01_底层规则/规则_战斗底层伤害与时间轴推演协议_v1.md` |
| 状态机制 | `策划文档/02_战斗系统/02_状态机制/*.md` |

---

## 2. 审核发现

| 编号 | 结论 | 发现 |
|---|---|---|
| C-01 | PASS | `adapter.mjs` 只读加载四张 `data/cards/partners/*.v1.json`，未写回卡牌数据。 |
| C-02 | PASS | 规范化效果覆盖 `damage`、`apply_dot_to_enemy`、`lifesteal_from_damage`、`buff` 四类效果；四张卡 4 个品质共生成 32 条规范化效果。 |
| C-03 | PASS | 触发规则覆盖 `cooldown_ready`、`self_gain_haste`、`other_ally_crit`、`while_deployed`；无主动/被动时允许 `null`。 |
| C-04 | PASS | 目标规则覆盖 `enemy_single`、`player_core`、`self`、`right_adjacent_damage_ally`、`single_deployed_damage_ally`。搬砖鼠的右侧相邻伤害伙伴与唯一伤害伙伴条件都有验证用例。 |
| C-05 | PASS | 属性修正覆盖 `damage_flat`、`poison_stack_bonus`、`cooldown_time_cap_bp`。其中 `cooldown_time_cap_bp` 仅作为部署期派生属性输出，完整冷却运行时消费留给后续系统。 |
| C-06 | PASS | 狗头人和嗜血狼的 `damage_type`、`can_crit`、`can_trigger_lifesteal` 待定字段按 `pending_null_rule` warning 保留，规范化效果继续生成，未静默推断成定稿规则。 |
| C-07 | PASS | 嗜血狼吸血按 `actual_hp_damage` 结算，护盾吸收不计入，且按主角生命上限截断，符合底层协议与吸血状态文档。 |
| C-08 | PASS | 适配器和验证器是 Node 工具层，不是 Godot 运行时；本次审核没有发现目标范围内的 `.gd`、`.tscn`、`.tres` 或 `project.godot` 改动。 |
| C-09 | NOTE | 当前工作树存在非本审核范围的 `.tscn` 已修改/未跟踪文件；该事实不能从当前上下文归因给 Agent B，本次归档结论不覆盖这些场景资源。 |

---

## 3. 待确认项

| 项目 | 状态 | 说明 |
|---|---|---|
| 狗头人 `damage_type` | 待主控确认 | 当前 JSON 为 `null`，验证输出 warning。 |
| 狗头人 `can_crit` | 待主控确认 | 当前 JSON 为 `null`，验证输出 warning。 |
| 狗头人 `can_trigger_lifesteal` | 待主控确认 | 当前 JSON 为 `null`，验证输出 warning。 |
| 嗜血狼 `damage_type` | 待主控确认 | 当前 JSON 为 `null`，验证输出 warning。 |
| 嗜血狼 `can_crit` | 待主控确认 | 当前 JSON 为 `null`，验证输出 warning。 |
| 搬砖鼠 `cooldown_time_cap_bp` 运行时消费 | 后续系统任务 | 当前适配器能输出派生属性修正，但仓库尚无完整战斗冷却运行时。 |

---

## 4. 验证证据

| 命令 | 结果 |
|---|---|
| `node tools/card_effect_adapter/validate_role1_cards.mjs` | 通过：`ok=true`，加载 4 张卡，生成 32 条规范化效果，5 条待确认 warning，0 error。 |
| `node tools/card_effect_adapter/validate_role1_cards.mjs --json` | 通过：warning 精确为狗头人 3 项、嗜血狼 2 项待定字段；`failures=[]`。 |
| `node --check tools/card_effect_adapter/adapter.mjs` | 通过：退出码 0。 |
| `node --check tools/card_effect_adapter/validate_role1_cards.mjs` | 通过：退出码 0。 |
| `git diff --check` | 通过：退出码 0。 |
| `git diff --cached --check` | 通过：退出码 0。 |
| `powershell -NoProfile -ExecutionPolicy Bypass -File tools/verify_text_encoding.ps1` | 通过：`UTF-8 encoding check passed.` |
| `powershell -NoProfile -ExecutionPolicy Bypass -File tools/verify_text_encoding.ps1 -Staged` | 通过：`UTF-8 encoding check passed.` |

---

## 5. Godot API 核验

本次审核没有审查、建议或修改任何 Godot API 符号，也没有修改 `.gd`、`.tscn`、`.tres` 或 `project.godot`。因此未进入 Godot API 符号查询流程。

为核验 B 的声明，已检查工作树中的 Godot 场景/资源路径：当前工作树确实存在非本审核范围的 `.tscn` 变更和未跟踪文件。由于这些文件不在本次指定审核范围内，且当前上下文无法把它们归因给 Agent B，本审核只记录事实，不对这些 Godot 场景资源做通过结论。

---

## 6. 归档结论

四张角色 1 伙伴卡的只读卡牌效果适配/验证层审核通过并归档：

| 卡牌 | card_id | 归档状态 |
|---|---|---|
| 蝴蝶守卫 | `butterfly_guard` | 已归档（2026-06-11） |
| 狗头人 | `kobold` | 已归档（2026-06-11） |
| 搬砖鼠 | `brick_mouse` | 已归档（2026-06-11） |
| 嗜血狼 | `blood_wolf` | 已归档（2026-06-11） |

归档边界：本结论只覆盖 `tools/card_effect_adapter/` 的只读数据适配与验证工具，以及四张卡当前 JSON/Markdown 口径的机制承接。完整 Godot 战斗运行时、时间轴事件队列、冷却系统消费与 UI 表现接入不属于本次归档范围。
